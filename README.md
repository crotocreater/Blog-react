mô hình MVC trong back-end 

hoạt động 
client -> router -> controller -> services (để lấy dữ liệu) -> utils(database)
                               -> module (sứ lý logic)     

-> view (trả về kết quả);







chức năng tài khoản người dùng 